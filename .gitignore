# dependencies
node_modules/

# runtime persistence (JsonStore output) — regenerated at runtime
data/

# server logs
*.log

# playwright e2e artifacts
test-results/
playwright-report/

# local editor / tool state
.claude/settings.local.json

# nested, unrelated project that carries its own git repository
pebble/
